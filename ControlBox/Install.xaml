<Window x:Class="ControlBox.Install"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:ControlBox"
        mc:Ignorable="d" 
        AllowDrop="True"
        Title="Install" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Command="{Binding OpenCommand}">Open...</Button>
        
        <TextBlock Grid.Row="1" Grid.Column="0"><Run Text="MainDll"/></TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MainDll}" />

        <ListBox Grid.Row="2" Grid.ColumnSpan="2" ItemsSource="{Binding Filenames}" SelectedItem="{Binding MainDll}"/>

        <WrapPanel Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right">
            <Button Margin="2" Command="{Binding InstallCommand}">Install</Button>
            <Button Margin="2" Command="{Binding Close}" Content="Close">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:CallMethodAction MethodName="Close" TargetObject="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </WrapPanel>

    </Grid>

</Window>
