<Window x:Class="ControlBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:ControlBox"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" Grid.ColumnSpan="3">
            <Button Margin="2" Command="{Binding ListCommand, IsAsync=True}">Query</Button>
            <Button Margin="2" Command="{Binding OpenInstallCommand}">Install...</Button>
        </WrapPanel>
        <GridSplitter Grid.Column="1" Grid.Row="1" Width="1"/>
        <TreeView ItemsSource="{Binding Services}" Grid.Row="1" Grid.Column="0" >
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type local:TreeNode}" ItemsSource="{Binding Children}">
                    <TextBlock>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDown">
                            <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:MainWindow}, Path=DataContext.DisplayCommand}"
                                                   CommandParameter="{Binding}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                        <Run Text="{Binding Name}"/>
                        <Run Text=" "/>
                        <Run Text="{Binding Value}"/>
                    </TextBlock>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <local:CallEditor Grid.Row="0" DataContext="{Binding CallData}"/>
            <WrapPanel Grid.Row="1" HorizontalAlignment="Right">
                <Button Margin="2" Command="{Binding CallCommand}">Call</Button>
                <Button Margin="2" Command="{Binding UninstallCommand}">Uninstall</Button>
            </WrapPanel>
        </Grid>
        
    </Grid>
</Window>
