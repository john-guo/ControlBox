<UserControl x:Class="ControlBox.CallEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControlBox"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="2" Text="Service" />
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="2" Text="{Binding Service}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="2" Text="Function"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="2" Text="{Binding Function}"/>

        <ItemsControl Grid.Row="2" Grid.ColumnSpan="2" ItemsSource="{Binding Data}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <TextBlock Margin="2" Text="{Binding Name}"/>
                        <TextBox Margin="2" Text="{Binding Value}" Width="100"/>
                    </WrapPanel>    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>            
    </Grid>
</UserControl>
